/* For loops have two semicolons inside that break up the for loops paramaters

for ( *initialization* ; *condition check if loop will continue *; *do this after each loop*) {
	*in each loop do this code*
}